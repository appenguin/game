#!/bin/bash
# Build signed release APK
# Requires gradle.properties with signing config

cd android && ./gradlew assembleRelease

if [ $? -eq 0 ]; then
    echo ""
    echo "Release APK: android/app/build/outputs/apk/release/app-release.apk"
fi
